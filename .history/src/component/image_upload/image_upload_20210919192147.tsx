import React from "react";
import styles from "./image_upload.module.css";

const ImageUpload = (props: any) => {
  return <button className={styles.uploadbutton}>Upload</button>;
};

export default ImageUpload;
